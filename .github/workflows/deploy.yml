name: Deploy Production to VPS

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  # VPS connection
  VPS_HOST: ${{ secrets.VPS_HOST }}
  VPS_USER: ${{ secrets.VPS_USER }}

  # Project config
  SITE_NAME: elliotreedmusic
  NODE_VERSION: "24"

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Set remote path
        run: echo "REMOTE_SITE_PATH=~/apps/${{ env.SITE_NAME }}" >> $GITHUB_ENV

      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Setup SSH key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.VPS_SSH_KEY }}

      - name: Add SSH host to known_hosts
        run: ssh-keyscan ${{ env.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build
      - name: Deploy via rsync
        run: |
          rsync -avz --delete \
            --exclude 'node_modules' \
            --exclude '.git' \
            --exclude '.github' \
            --exclude 'src' \
            --exclude '.env.production' \
            ./dist/ \
            ${{ env.VPS_USER }}@${{ env.VPS_HOST }}:${{ env.REMOTE_SITE_PATH }}/

          rsync -avz \
            ./package.json ./package-lock.json \
            ${{ env.VPS_USER }}@${{ env.VPS_HOST }}:${{ env.REMOTE_SITE_PATH }}/

      - name: Install production dependencies on VPS
        run: |
          ssh ${{ env.VPS_USER }}@${{ env.VPS_HOST }} \
            "cd ${{ env.REMOTE_SITE_PATH }} && npm ci --omit=dev"VPS_USER }}@${{ env.VPS_HOST }}:${{ env.REMOTE_SITE_PATH }}/

      - name: Restart app
        run: |
          ssh ${{ env.VPS_USER }}@${{ env.VPS_HOST }} \
            "pm2 restart ${{ env.SITE_NAME }} || pm2 start ${{ env.REMOTE_SITE_PATH }}/server/entry.mjs --name ${{ env.SITE_NAME }}"

      - name: Deployment summary
        run: |
          echo "🚀 Deployed to ${{ env.VPS_HOST }}"
          echo "📁 Site: ${{ env.SITE_NAME }}"
