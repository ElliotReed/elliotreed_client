---
import { getCollectionPaths } from "@/utils/getCollectionPaths";
import CollectionArticlePage from "@/page_components/CollectionArticlePage.astro";

export async function getStaticPaths() {
    return getCollectionPaths("recording");
}

const { article } = Astro.props;
---

<CollectionArticlePage collectionName="recording" article={article} />
